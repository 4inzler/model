# Copy to .env and fill in values.

# ============================================================
# REQUIRED: Discord
# ============================================================
DISCORD_TOKEN=PASTE_YOUR_TOKEN_HERE

# ============================================================
# REQUIRED: Local Model Paths (HuggingFace format)
# ============================================================
# General conversation model (e.g., Qwen2.5-7B, Llama-3, Mistral)
SEL_GENERAL_MODEL=/home/USER/models/FuseChat-Qwen-2.5-7B-Instruct

# Coding specialist model (e.g., DeepSeek-Coder, Qwen2.5-Coder)
SEL_CODER_MODEL=/home/USER/models/Qwen2.5-Coder-7B-Instruct

# ============================================================
# OPTIONAL: Model Behavior
# ============================================================
# Quantization: 4bit (saves 75% VRAM), 8bit, or empty for fp16
SEL_QUANT=4bit

# Single backend mode: swap models to save VRAM (1=yes, 0=no)
SEL_SINGLE_BACKEND=1

# Maximum tokens to generate per response
SEL_MAX_NEW_TOKENS=256

# Vision model for attachment reactions (set to "off" to disable)
SEL_VISION_MODEL=Qwen/Qwen2-VL-7B-Instruct

# Optional overrides for persistent state locations
SEL_MEMORY_PATH=sel_memory.json
SEL_STATE_PATH=sel_state.json
SEL_PERSONA_PATH=sel_persona.json
SEL_CHANNEL_PROFILE_PATH=sel_channels.json

# Memory decay tuning (seconds)
SEL_SHORT_TERM_TTL=21600
SEL_STICKY_TTL=604800
SEL_IMAGE_TTL=172800

# ============================================================
# OPTIONAL: Personality & Behavior
# ============================================================
# Personality mode: casual, deep_focus, playful, quiet_companion
SEL_MODE=casual

# Enable streaming responses (1=yes, 0=no)
SEL_STREAM=1

# Show `[mood: ...]` line after each response (1=yes, 0=no)
SEL_SHOW_MOOD=1

# Reply even when not mentioned in channels (1=yes, 0=no)
SEL_RESPOND_TO_ALL=0

# Minimum engagement score required to auto-reply in channels (0.0-1.0)
SEL_AUTO_REPLY_CHANCE=0.45

# Minimum seconds between spontaneous replies per channel
SEL_AUTO_REPLY_COOLDOWN=8

# Grace period (seconds) where SEL keeps replying to the same user even without mentions
SEL_FOLLOWUP_WINDOW=120

# Enable multi-stage private reasoning (1=yes, 0=no)
SEL_DEEP_THINKING=1

# Number of internal reflection passes before replying
SEL_REFLECTION_STEPS=2

# Max tokens per reflection pass
SEL_REFLECTION_TOKENS=150

# How many recent turns to feed into prompts
SEL_PROMPT_HISTORY_TURNS=8

# Logging level for CLI/Discord tooling (DEBUG, INFO, WARNING, ERROR)
SEL_LOG_LEVEL=INFO

# Ignore messages older than X seconds (prevents backlog processing)
SEL_DROP_OLD_SECONDS=30

# Enable periodic check-ins in channels (1=yes, 0=no)
SEL_CHECKINS=0
# Minutes of channel silence before a check-in
#SEL_CHECKIN_MINUTES=45
# Minimum minutes between check-ins per channel
#SEL_CHECKIN_GAP_MINUTES=180
# DM nudge window (minutes) when SEL feels bored
#SEL_DM_CHECKIN_MINUTES=60

# ============================================================
# OPTIONAL: Text-to-Speech (TTS)
# ============================================================
# Path to Piper TTS model (.onnx file)
PIPER_MODEL=/home/USER/models/piper/en_US-amy-medium.onnx

# Piper binary name (default: piper)
PIPER_BINARY=piper

# Enable TTS by default in voice channels (1=yes, 0=no)
SEL_TTS_DEFAULT=1

# ============================================================
# OPTIONAL: Speech-to-Text (STT)
# ============================================================
# Whisper model size: tiny, base, small, medium, large
# Smaller = faster but less accurate
STT_MODEL=base.en

# ============================================================
# OPTIONAL: Legacy Settings (for compatibility)
# ============================================================
SEL_DISABLE_PRIVILEGED_INTENTS=0
SEL_BOT_NAME=SEL

# ============================================================
# Docker Deployment (if using Docker with volume mounts)
# ============================================================
# Mount your models directory: -v /home/USER/models:/models
# Then use these paths:
# SEL_GENERAL_MODEL=/models/FuseChat-Qwen-2.5-7B-Instruct
# SEL_CODER_MODEL=/models/Qwen2.5-Coder-7B-Instruct
# PIPER_MODEL=/models/piper/en_US-amy-medium.onnx

# ============================================================
# SEL Enhanced Features (automatically managed)
# ============================================================
# These files are created and managed automatically:
# - sel_memory.json (hierarchical memory with TTL)
# - sel_beliefs.json (evolving belief system)
# - sel_knowledge.json (accumulated knowledge)
# - sel_age_state.json (age and rebirth tracking)
# - sel_state.json (hormone levels and mood)
# - sel_persona.json (long-term persona traits)
